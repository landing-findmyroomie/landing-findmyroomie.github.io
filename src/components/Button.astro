---
interface Props {
  class1: string;
  url: string;
  text?: string;
}

const { class1, url, text } = Astro.props;
---

<button
  class=`${class1} shadow-lg text-white text-sm py-2 px-4 rounded-full cursor-pointer`
>
  <a href={url}>{text ? text : 'Join the waitlist'}</a>
</button>
